Original prompt: 有一个问题你帮我看看，我在我这个电脑的浏览器里打开这个游戏，UI都正常。我另一个电脑里打开这个游戏，比如剧情UI就会放大（图鉴是正常的），就是类似于剧情UI，或者剧情系统入口点开的UI就会变大

- 2026-03-26: 排查到剧情相关样式大量使用 `rem`/固定像素，而图鉴新稿主要使用 `cqw` 容器单位，所以不同电脑/浏览器默认字号不一致时，剧情 UI 更容易出现“整体变大”的现象。
- 2026-03-26: 准备先用最小修复统一根字号与文本缩放策略，避免浏览器默认字号/自动文本放大影响剧情与剧情入口弹层。
- 2026-03-26: 已在 `style.base.css` 增加 `html { font-size: 16px; text-size-adjust: none; -webkit-text-size-adjust: none; }`。
- 2026-03-26: 本地起了一个临时 `http.server` 做静态检查准备，但内置 Playwright 客户端脚本因当前环境缺少可直接解析的 `playwright` 包而无法按技能要求跑通；这次先保留为代码级修复，建议在两台电脑上各自强刷后做实机对比。

- 2026-03-30: Added cache-busting in `index.html` for static hosting refreshes, then traced a regression where dynamic script injection rendered a literal `<\/script>` and swallowed the rest of the boot scripts.
- 2026-03-30: Reproduced locally with `http.server` and Playwright. During the broken state, `window.WynneZooAppShell`, `window.WynneStoryPlayer`, and `window.WynneZooEconomy` were all missing, which is why entering a user ID could not proceed into the game.
- 2026-03-30: Fixed the injected script closing tag to `'<script ...><' + '/script>'` and removed the duplicate `style.story.css` include. Retest showed the core globals recovered and the app could auto-enter the post-login flow again.
- 2026-04-02: Used the Figma node `497:186` as the placement reference for the red panda tier-6 habitat. Updated `js/zoo/zoo-home.js` so level 6 uses its own hotspot bounds plus cropped image offsets (`top -53.35%`, `height 191.49%`) instead of the shared full-image placement, and updated both duplicated `.zoo-habitat-scene` rules in `style.zoo-home.css` to read per-habitat `object-fit` / `object-position` CSS vars.
- 2026-04-02: Tried to run the Playwright verification loop again, but this machine still does not have the `playwright` package available for the bundled client script. JS syntax was validated with `node`, but final visual confirmation still needs an in-browser check on the zoo home screen with the red panda habitat at tier 6.
- 2026-04-03: Updated `js/zoo/zoo-economy.js` so the red panda habitat keeps level 1 on first build with `habitat-level-1.webp`, then automatically swaps level-1 art to `habitat-level-1-redpanda.webp` once `storyFlags['post-build-red-panda']` is set after the special post-build story completes.
- 2026-04-03: Added habitat-art preloading in `js/zoo/zoo-home.js` for `habitat-level-1-redpanda.webp`. The image is now warmed as soon as the red panda habitat build succeeds and also during subsequent zoo-home renders before the post-build story is marked complete, reducing the visible flash when the level-1 art swaps after `post-build-red-panda`.
- 2026-04-03: Added a very short habitat art swap animation in `js/zoo/zoo-home.js` + `style.zoo-home.css`. The zoo habitat image now gets an `is-art-transitioning` class only when the same habitat's rendered art src actually changes, so the post-story red panda level-1 image swap fades in gently without making normal re-renders animate.
- 2026-04-08: Fixed a zoo-home stage cache bug in `js/zoo/zoo-home.js`. The lightweight `stageKey` used to skip DOM rebuilds did not include the current habitat art src, so after `post-build-red-panda` marked the story flag and `stageAssets.level1` switched to `habitat-level-1-redpanda.webp`, the main habitat stage could stay stuck on the old image. The cache key now includes `getHabitatArt(displayHabitat)`.
